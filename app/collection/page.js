import { React } from "react";
import Collection from "../../components/collection/Collection";

export const metadata = {
  title: "collection",
};

const Page = () => {
  return <Collection />;
};

export default Page;

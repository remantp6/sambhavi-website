import { React } from "react";
import Contact from "../../components/contact/Contact";

export const metadata = {
  title: "contact",
};

const Page = () => {
  return <Contact />;
};

export default Page;
